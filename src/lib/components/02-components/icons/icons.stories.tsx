// Temporary file to avoid Storybook error
import type { Meta } from '@storybook/react';

const meta = {
  title: 'Elements/Icons',
} satisfies Meta;

export default meta;

export const Default = () => {
  return <h2>Icons</h2>;
};
